; Development PHP-FPM pool overrides. Bare directives continue the [www] pool the shutdown drop-in
; left open. Worker output reaches `docker logs` undecorated, so error_log and var_dump from a
; request show up while developing.

catch_workers_output = yes
decorate_workers_output = no
